---
title: Set (Insiemi)
description: Come raccogliere elementi unici, eliminare duplicati e confrontare insiemi di valori.
---

## Cosa sono i set?

Un **set** (insieme) è una raccolta di elementi con due regole fondamentali:

1. **Nessun duplicato**: ogni elemento può comparire al massimo una volta
2. **Nessun ordine garantito**: gli elementi non hanno una posizione fissa

Un set è simile a un sacchetto di palline colorate: sai quali colori contiene, ma non puoi chiedere l'elemento in una posizione precisa.

## Creare un set

Si usano le **parentesi graffe** `{}`, come per i dizionari, ma senza le chiavi:

```python
frutta = {"mela", "banana", "ciliegia"}
numeri = {1, 2, 3, 4, 5}
```

:::caution
`{}` da solo crea un **dizionario vuoto**, non un set vuoto! Per creare un set vuoto usa `set()`:

```python
vuoto = set()      # set vuoto ✓
anche_vuoto = {}   # dizionario vuoto, non un set! ✗
```

:::

I duplicati vengono eliminati automaticamente:

```python
con_duplicati = {1, 2, 2, 3, 3, 3}
print(con_duplicati)  # {1, 2, 3}: solo valori unici
```

## Aggiungere e rimuovere elementi

```python
frutta = {"mela", "banana"}

frutta.add("ciliegia")      # aggiunge un elemento
frutta.remove("banana")     # rimuove (dà errore se non esiste)
frutta.discard("uva")       # rimuove senza errore se non esiste
```

## Controllare se un elemento è presente

```python
frutta = {"mela", "banana", "ciliegia"}
print("mela" in frutta)     # True
print("uva" in frutta)      # False
```

La ricerca di un elemento in un set è in genere più veloce che in una lista.

## Operazioni sugli insiemi

I set supportano anche le operazioni della matematica degli insiemi:

```python
A = {1, 2, 3, 4}
B = {3, 4, 5, 6}
```

**Unione**: tutti gli elementi di A e di B.

```python
print(A | B)   # {1, 2, 3, 4, 5, 6}
```

**Intersezione**: solo gli elementi presenti in entrambi.

```python
print(A & B)   # {3, 4}
```

**Differenza**: gli elementi in A ma non in B.

```python
print(A - B)   # {1, 2}
```

## Eliminare i duplicati

Per eliminare i duplicati da una lista puoi convertirla in set e poi riconvertirla in lista:

```python
lista = [1, 2, 2, 3, 3, 4, 1]
lista_pulita = list(set(lista))
print(lista_pulita)  # [1, 2, 3, 4]
```

:::note
L'ordine originale non viene preservato, perché i set non hanno un ordine fisso.
:::

## Limiti dei set

- Non puoi accedere agli elementi per posizione: `frutta[0]` causa errore
- Gli elementi devono essere immutabili: puoi mettere numeri, stringhe, tuple, ma non liste
