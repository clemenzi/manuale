---
title: Date e tempo
description: Come rappresentare date, orari e intervalli nei programmi PHP.
---

## Perché le date richiedono attenzione

Una data sembra semplice, ma può includere giorno, mese, anno, ora, minuti, secondi e fuso orario. PHP offre `DateTime` per gestire questi dati in modo ordinato.

## Creare una data

```php
<?php
$oggi = new DateTime();

echo $oggi->format('d/m/Y');
```

`new DateTime()` crea un oggetto con data e ora attuali. `format` stabilisce come visualizzarlo.

## Creare una data specifica

```php
<?php
$scadenza = new DateTime('2026-06-15');

echo $scadenza->format('d/m/Y');
```

Output:

```text
15/06/2026
```

## Fusi orari

Il fuso orario indica a quale zona del mondo si riferisce l'orario.

```php
<?php
$roma = new DateTime('now', new DateTimeZone('Europe/Rome'));

echo $roma->format('H:i');
```

Usare il fuso corretto evita risultati inattesi, soprattutto nei siti usati da persone in paesi diversi.

## Timestamp

Un timestamp è un numero che rappresenta un momento nel tempo.

```php
<?php
echo time();
```

È utile per i confronti tecnici, ma nel codice `DateTime` è spesso più chiaro.

## Confrontare date

```php
<?php
$oggi = new DateTime('2026-05-02');
$scadenza = new DateTime('2026-05-10');

if ($oggi < $scadenza) {
    echo "Sei ancora in tempo";
}
```

PHP confronta i due momenti e stabilisce quale viene prima.

## Aggiungere o togliere tempo

Con `modify` puoi spostare una data in avanti o indietro.

```php
<?php
$scadenza = new DateTime('2026-05-02');
$scadenza->modify('+7 days');

echo $scadenza->format('d/m/Y');
```

Output:

```text
09/05/2026
```

È utile per scadenze, promemoria e periodi di prova.

## Differenza tra due date

```php
<?php
$inizio = new DateTime('2026-05-02');
$fine = new DateTime('2026-05-10');

$differenza = $inizio->diff($fine);

echo $differenza->days;
```

Output:

```text
8
```

`diff` restituisce un intervallo. La proprietà `days` contiene il numero totale di giorni tra le due date.

## Errore comune: formato ambiguo

Date come `05/06/2026` possono essere lette come 5 giugno o 6 maggio, a seconda del contesto. Quando crei date nel codice, preferisci formati chiari come `2026-06-05`.
