---
title: Classi e oggetti
description: I concetti essenziali della programmazione a oggetti in PHP.
---

## Perché usare gli oggetti

Quando un programma cresce, variabili e funzioni sparse possono diventare difficili da seguire. La programmazione a oggetti riunisce dati e comportamenti collegati.

## Classe e oggetto

Una **classe** è un modello. Un **oggetto** è un'istanza concreta creata da quel modello.

```php
<?php
class Prodotto {
    public string $nome;
    public float $prezzo;
}

$quaderno = new Prodotto();
$quaderno->nome = "Quaderno";
$quaderno->prezzo = 3.50;

echo $quaderno->nome;
```

`new Prodotto()` crea un'istanza della classe.

## Proprietà

Le proprietà sono variabili definite dentro una classe.

```php
public string $nome;
public float $prezzo;
```

`public` significa che puoi leggere e modificare la proprietà dall'esterno.

## Metodi

I metodi sono funzioni definite dentro una classe.

```php
<?php
class Prodotto {
    public string $nome;
    public float $prezzo;

    public function descrizione(): string {
        return $this->nome . " costa " . $this->prezzo . " euro";
    }
}

$prodotto = new Prodotto();
$prodotto->nome = "Penna";
$prodotto->prezzo = 1.20;

echo $prodotto->descrizione();
```

`$this` indica l'oggetto corrente.

## Quando iniziare a usarli

All'inizio puoi scrivere programmi semplici con variabili, array e funzioni. Gli oggetti diventano utili quando vuoi rappresentare concetti del progetto, come utenti, prodotti, ordini o articoli.

## Oggetti e array associativi

Un array associativo può descrivere un prodotto:

```php
<?php
$prodotto = [
    "nome" => "Penna",
    "prezzo" => 1.20,
];
```

Una classe rende esplicito che quei dati appartengono a un concetto preciso.

```php
<?php
class Prodotto {
    public string $nome;
    public float $prezzo;
}
```

La classe dice che un prodotto ha un nome e un prezzo. È utile quando il progetto cresce.

## Creare più oggetti

```php
<?php
$penna = new Prodotto();
$penna->nome = "Penna";
$penna->prezzo = 1.20;

$quaderno = new Prodotto();
$quaderno->nome = "Quaderno";
$quaderno->prezzo = 3.50;
```

`$penna` e `$quaderno` sono due oggetti diversi creati dallo stesso modello.

## Errore comune: dimenticare new

Per creare un oggetto devi usare `new`.

```php
<?php
$prodotto = new Prodotto();
```

Senza `new` non stai creando un'istanza della classe.
