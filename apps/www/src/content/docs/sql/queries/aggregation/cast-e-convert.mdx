---
title: CAST e CONVERT
description: Cambiare il tipo di un valore quando serve adattarlo al contesto.
---

## Convertire un valore

**Un valore può avere il tipo sbagliato per l'operazione che vuoi eseguire.** In questi casi puoi convertirlo.

`CAST` e, in alcuni database, `CONVERT`, servono proprio a cambiare il tipo di un valore.

```sql
SELECT CAST(totale AS INT)
FROM ordini;
```

Questa query prende il valore `totale` e lo converte in intero.

## Perché può servire

Le conversioni sono utili quando:

- devi confrontare valori che arrivano con tipi diversi
- vuoi formattare un risultato
- vuoi troncare o adattare un valore
- stai lavorando con dati importati in modo poco uniforme

## `CAST` e `CONVERT` non sono gemelli perfetti

**`CAST` è più standard e portabile.** Di solito è la scelta migliore quando vuoi usare SQL compatibile con più database.

**`CONVERT` esiste in diversi dialetti, ma la sintassi può cambiare.** Usalo quindi tenendo conto del database scelto.
